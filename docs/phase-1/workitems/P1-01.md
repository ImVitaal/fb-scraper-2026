# P1-01 — Python package, quality tools, CI, and skeleton

| Field | Value |
|---|---|
| id | P1-01 |
| title | Python package, quality tools, CI, and skeleton |
| state | complete |
| owner | root coordinator |
| dependencies | P1-00 |
| owned_paths | `pyproject.toml`; `uv.lock`; `.python-version`; `.gitignore`; `.github/workflows/`; package and CLI skeleton; skeleton tests; `SECURITY.md`; `CONTRIBUTING.md` |
| acceptance | Install, lint, type, test, and build receipts |
| commands | See evidence receipt; all acceptance commands passed |
| evidence_paths | `docs/phase-1/evidence/P1-01-receipt.md` |
| changed_files | `.gitignore`; `.python-version`; `pyproject.toml`; `uv.lock`; `.github/workflows/quality.yml`; `src/app/__init__.py`; `src/app/cli/__init__.py`; `src/app/cli/main.py`; `tests/unit/test_package_skeleton.py`; `SECURITY.md`; `CONTRIBUTING.md`; this work-item record; `docs/phase-1/evidence/P1-01-receipt.md` |
| open_defects | None within P1-01. Command bodies are intentionally skeleton handlers; collection behavior belongs to downstream work items. |

## Dependency check

`P1-00` is complete in commit `bea7be1`.

## Completion

The `src/` package is installable through `uv`, exposes version `0.1.0`, and
provides the Phase 1 top-level and session command groups. Ruff, `ty`, pytest
with the configured 80% coverage gate, build, dependency audit, and secret
scan all pass. The exact commands, tool versions, test results, artifact
hashes, and limitations are recorded in the evidence receipt.

No commit was created. The repository remains on `main` at the P1-00 baseline
commit with the P1-01 files present as working-tree additions.
