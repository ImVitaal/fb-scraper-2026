# P1-02 — Contracts, states, migrations, and repositories

| Field | Value |
|---|---|
| id | P1-02 |
| title | Contracts, states, migrations, and repositories |
| state | complete |
| owner | root coordinator |
| dependencies | P1-01 |
| owned_paths | `src/app/contracts/`; `src/app/storage/`; `migrations/`; P1-02 tests and records |
| acceptance | Contract and state tests; ordered migration round-trip; repository and transition tests |
| commands | See evidence receipt; all acceptance commands passed |
| evidence_paths | `docs/phase-1/evidence/P1-02-receipt.md` |
| changed_files | Contracts package; storage package; ordered migration and packaged copy; unit and integration tests; this record; evidence receipt |
| open_defects | None within P1-02 |

## Dependency check

P1-01 is complete in commit `959768a`.

## Scope

Define the versioned Group, Post, Comment, evidence, state, and observation
contracts. Add ordered SQLite migrations and repositories for durable canonical
records, observations, and job-state transitions.

## Exclusions

- Raw capture byte storage and retention.
- Session secret encryption.
- Source parsing.
- Browser or network adapters.
- CLI behavior beyond the existing skeleton.

## Completion

The exact Phase 1 state vocabularies and version 1 canonical contracts are
implemented. SQLite migration 001 creates every required persistence category.
Repositories persist Group, Post, Comment, counter-history, capture metadata,
and valid job transitions.

The migration applies once, remains idempotent after reopening, and operates
from an installed wheel. The repository and packaged SQL copies are byte-equal.
