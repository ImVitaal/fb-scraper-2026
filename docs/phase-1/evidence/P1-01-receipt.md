# P1-01 evidence receipt

## Working-tree identity

- Repository: `C:\Users\teqhv\fb scraper`
- Branch: `main`
- Baseline `HEAD`: `bea7be1c59d8b13dc1ba2cb6dfee01cd79165fdd`
- Dependency: P1-00 complete at `bea7be1`
- Commit created during this work: none
- Scope: P1-01 owned paths only; shared contracts and migrations were not edited.

## Tool versions

Captured with:

```powershell
uv --version
uv run python --version
uv run ruff --version
uv run ty --version
uv run pytest --version
uv run pip-audit --version
uv run detect-secrets --version
uv run python -m build --version
```

Results:

| Tool | Version |
|---|---|
| uv | 0.11.28 |
| Python | 3.12.13 |
| Ruff | 0.16.0 |
| ty | 0.0.64 |
| pytest | 9.1.1 |
| pip-audit | 2.10.1 |
| detect-secrets | 1.5.0 |
| build | 1.5.0 |

## Exact commands and results

| Command | Result |
|---|---|
| `uv sync --all-groups` | PASS; resolved and installed 56 packages |
| `uv sync --locked --all-groups` | PASS; lockfile verified, 56 packages checked |
| `uv run pytest tests/unit/test_package_skeleton.py -q` | PASS; 3 passed; 89.74% total coverage; configured 80% gate passed |
| `uv run pytest -q` | PASS; 3 passed; 89.74% total coverage |
| `uv run ruff format src tests` | PASS; 4 files reformatted |
| `uv run ruff format --check .` | PASS; 17 files already formatted |
| `uv run ruff check .` | PASS; all checks passed |
| `uv run ty check` | PASS; all checks passed |
| `uv build` | PASS; wheel and source distribution built |
| `uv run pip-audit` | PASS; no known vulnerabilities; local project skipped because it is not published on PyPI |
| `uv run detect-secrets scan --all-files --exclude-files '(^|[\\/])(\\.venv|\\.pytest_cache|\\.ruff_cache|\\.ty|dist|build|htmlcov)([\\/]|$)'` | PASS; `"results": {}` |
| `uv run pgscan --help` | PASS; lists `run`, `session`, `inspect`, `resume`, `replay`, and `clean` |
| `uv run pgscan --version` | PASS; `pgscan 0.1.0` |

The initial unscoped Ruff format check also inspected the unowned research
document `TOP_GITHUB_FACEBOOK_SCRAPERS_ARCHITECTURE_ANALYSIS.md`. The project
configuration now excludes that pre-existing document without changing it;
the final repository-wide command passes.

## Artifacts

Generated by `uv build` in the ignored `dist/` directory:

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `dist/private_group_scanner-0.1.0-py3-none-any.whl` | 5577 | `E1D4D44D80CD5761D8E02F0E0086812198E2F4F86AD4CC1821DF0019D77D04F9` |
| `dist/private_group_scanner-0.1.0.tar.gz` | 4683 | `29A1F5AB409B7EB406DF842A6D332D207154FC10A7A1FAD50C271362DD674FCD` |

Additional reproducibility hashes:

| File | SHA-256 |
|---|---|
| `uv.lock` | `EEBCAF78F2756913E0AEA66C1788C5A63BBD55210B5170FDD6E101DC7D106BF1` |
| `pyproject.toml` | `AAC3778541065A26524B51E32421E59D77B1F222CCDBD81CD41DC29D78C99597` |
| `.github/workflows/quality.yml` | `434718864D16DF3378D4BD15B001AF20E95EC0D7384953AE0B74FBCBE2C0F1AF` |

## Acceptance-gate mapping

- **Install:** `uv sync --locked --all-groups` passed on Python 3.12.13.
- **Package and CLI skeleton:** version and help smoke tests passed; 3 unit
  tests passed.
- **Quality:** Ruff format/check and `ty` passed.
- **Coverage:** 89.74%, above the configured 80% minimum.
- **Build:** wheel and source distribution exist and are hashed above.
- **Dependency security:** pip-audit reported no known vulnerabilities in
  auditable dependencies.
- **Secret hygiene:** detect-secrets returned an empty results object after
  excluding generated environments and build/cache directories.
- **CI:** `.github/workflows/quality.yml` runs the same Windows 3.12 quality,
  build, audit, and secret-scan gates.

## Limitations and open defects

There are no open P1-01 defects. The CLI handlers are deliberately no-op
skeleton entry points; session, collection, replay, resume, and retention
behavior are downstream work-item responsibilities. The local package itself
is skipped by pip-audit because it is not a PyPI publication; dependency
auditing still completed with no known vulnerabilities.
